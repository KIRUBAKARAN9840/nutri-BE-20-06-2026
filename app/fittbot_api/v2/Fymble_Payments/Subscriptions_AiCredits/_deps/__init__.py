"""
v2 dependency facade.

Every v1 import used by Subscriptions_AiCredits is re-exported from
this package. When v1 is eventually retired, only these files need
to change — the rest of v2 stays untouched.
"""
